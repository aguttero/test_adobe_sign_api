# opencode 
opencode --port 64319

## TODO
# main.py
* DB Health check at start
* DB healtk close flag at end
* refactor date calc to utils.py
* complete fetch users
* persist new users only
* fetch agrements by user for new date range
* for each user in the Users table fetch aßgreements that match criteria: (status=SIGNED and createdDate in DateRange)
* persist new agreements




